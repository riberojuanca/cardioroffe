import CarouselHeader from "./CarouselHeader";

const Header = () => {
  return (
    <section className="text-white font-semibold text-xl">
      <div>
        <CarouselHeader />
      </div>
    </section>
  );
};

export default Header;
