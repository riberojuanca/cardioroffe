import CarouselHeader from "./CarouselHeader";

const Header = () => {
  return (
    <section className="w-full mt-24 mx-auto bg-black h-fit flex justify-center items-center text-white font-semibold text-xl">
      <div>
        <CarouselHeader />
      </div>
    </section>
  );
};

export default Header;
